create table DEBTOR_DEBIT_PAYMENT(
    ID        BIGINT  NOT NULL AUTO_INCREMENT,
    PAYMENT_ID          VARCHAR(256) NOT NULL,
    SENDER_ROUTING_NUM          VARCHAR(256) NOT NULL,
    SENDER_ACCT_NUM          VARCHAR(256) NOT NULL,
    SENDER_FIRST_NAME          VARCHAR(256) NOT NULL,
    SENDER_LAST_NAME          VARCHAR(256) NOT NULL,
    SENDER_EMAIL          VARCHAR(256) NOT NULL,
    SENDER_CELL_PHONE         VARCHAR(256) NOT NULL,
    PAYMENT_AMOUNT          VARCHAR(256) NOT NULL,
    RECEIVER_FIRST_NAME          VARCHAR(256) NOT NULL,
    RECEIVER_LAST_NAME          VARCHAR(256) NOT NULL,
    RECEIVER_EMAIL          VARCHAR(256) NOT NULL,
    RECEIVER_CELL_PHONE         VARCHAR(256) NOT NULL,
    RECEIVER_ROUTING_NUM          VARCHAR(256) NOT NULL,
    RECEIVER_ACCT_NUM          VARCHAR(256) NOT NULL,
    STATUS          VARCHAR(256) NOT NULL,
    MESSAGE_STATUS_REPORT_ID          VARCHAR(256) NOT NULL,
    PRIMARY KEY (ID)
);
