create table DEBTOR_CREDIT_PAYMENT(
    ID        BIGINT  NOT NULL AUTO_INCREMENT,
    PAYMENT_ID          VARCHAR(256) NOT NULL,
    SENDER_ROUTING_NUM          VARCHAR(256) NULL,
    SENDER_ACCT_NUM          VARCHAR(256) NULL,
    SENDER_FIRST_NAME          VARCHAR(256) NULL,
    SENDER_LAST_NAME          VARCHAR(256) NULL,
    SENDER_EMAIL          VARCHAR(256) NULL,
    SENDER_CELL_PHONE         VARCHAR(256) NULL,
    PAYMENT_AMOUNT          VARCHAR(256) NULL,
    RECEIVER_FIRST_NAME          VARCHAR(256) NULL,
    RECEIVER_LAST_NAME          VARCHAR(256) NULL,
    RECEIVER_EMAIL          VARCHAR(256) NULL,
    RECEIVER_CELL_PHONE         VARCHAR(256) NULL,
    RECEIVER_ROUTING_NUM          VARCHAR(256) NULL,
    RECEIVER_ACCT_NUM          VARCHAR(256) NULL,
    STATUS          VARCHAR(256) NULL,
    MESSAGE_STATUS_REPORT_ID          VARCHAR(256) NULL,
    PRIMARY KEY (ID)
);
